// for loop

const endpoint =5;
for(let start = 1; start <= endpoint; start++ ) {
    // console.log(start);
}

let product = 1;
for(let start = 1; start <= endpoint; start++ ) {
    product = product * start;
}
// console.log(product);


