parse - > 1: If count 0 
              -> empty string and exit;             
          2: if one file               
              -> read 
          3: if multiple file                
              -> call read multiple times
          4: if no file
              -> read from stdin 

             cut ->
                  -n: cut by line
                  -c: cut by byte

                  send into stdout | stderr -> -q: don't show file name.
                                                single file | stdin : don't show file name.
                                                default: show file name.  

                                                