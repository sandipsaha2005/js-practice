// while loop

const noRange = 4;
let initialNo = 1;
let product=1;

while ( initialNo <= noRange ) {
    product = product * initialNo;
    initialNo = initialNo +1;
}

let startingPoint = 1;
const endPoint = 10;
while ( startingPoint <= endPoint ) {
    console.log(startingPoint);
    startingPoint++
}
